# Optional: only set if your booking API is on a different URL.
# On Vercel (same deployment as the app), leave this unset – the app uses /api/send-booking automatically.
# VITE_BOOKING_API_URL=https://your-app.vercel.app/api/send-booking
